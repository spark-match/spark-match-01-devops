# Placeholder so the directory survives checkout. Real bats tests land in
# PR #2 (composite actions) and PR #4 (reconciler). This file documents
# the conventions used by those future tests.
#
# Layout:
#
#   tests/
#     bats/
#       composite-actions.bats      # PR #3: validate.sh, run.sh
#       reconciler-check.bats       # PR #4: configure-repo-rulesets.sh --check
#       reconciler-apply.bats       # PR #4: configure-repo-rulesets.sh --apply
#       reconciler-teams.bats       # PR #4: team slug resolution
#       reconciler-backup.bats      # PR #4: backup + restore round-trip
#       reconciler-edges.bats       # PR #4: edge cases (missing team, bad manifest)
#
# Runner: bats-core 1.10+ via bats-core/bats-action
# Mocking: stub functions in `tests/bats/helpers/` (created per-PR)
